import { CPP_IMAGE, PYTHON_IMAGE } from "../utils/constants";

export const LANG_CONFIG= {
  python: {
    timeout: 5000,
    imageName: PYTHON_IMAGE
  },

  cpp: {
    timeout: 3000,
    imageName: CPP_IMAGE
  }
}